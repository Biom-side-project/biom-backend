

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/biom
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com:
      biom: debug
    org:
      springframework:
        security: debug