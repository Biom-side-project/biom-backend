server:
  port: 8130

  error:
    include-exception: true
    include-message: always
    include-stacktrace: never


users:
  jwt:
    secret: biomdevrandom
  oauth2:
    google:
      resource-url: https://www.googleapis.com/oauth2/v1/userinfo?alt=json
